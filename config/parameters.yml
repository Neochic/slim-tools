imports:
    - { resource: 'database.yml' }
    - { resource: 'doctrine.yml' }
parameters:
  core: &core.default
    mode: dev
    log.enabled: true,
    log.level: error
  routers:
    - router.rest
    - router.media
  middlewares: []
